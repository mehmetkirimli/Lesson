using Microsoft.AspNetCore.SignalR;

namespace Lesson.Hubs
{
    // Lesson: SignalR Hub (Merkez)
    // Hub, sunucu (API) ile istemciler (clients - web tarayıcıları, mobil uygulamalar) arasındaki çift yönlü, gerçek zamanlı iletişimin ana merkezidir.
    // İstemciler bu Hub'a "abone olurlar".
    public class ProductHub : Hub
    {

    }
}
